# Release Checkpoint

A small release ledger built on `WorkManager`.

```bash
python3 examples/projects/release_checkpoint/app.py
```

Use it as a starting point for release automation where every step should be
claimable, checkpointed, auditable, and resumable with a persistent store.
