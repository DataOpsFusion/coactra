# Approval Routing

Policy-gated collaboration before A2A transport.

```bash
python3 examples/projects/approval_routing/app.py
```

The local transport records calls. Production code should use the official A2A
adapter with verifier and tenant policy configured explicitly.
