# Research Workspace

A scoped workspace for files, reports, handoff notes, and capability metadata.

```bash
python3 examples/projects/research_workspace/app.py
```

Local command execution is not enabled. If you enable it in your own app, use an
explicit command policy and treat the workspace as trusted-local only.
